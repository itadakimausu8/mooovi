<div class="user_page">
  <header class="user_page-header">
    <div class="yjContainer">
      <a href="">
        <div class="user_image"><%= image_tag  current_user.avatar %></div>
        <!-- image_tagメソッドはassets/imagesフォルダに入っている画像を呼び出すことができるメソッドである。
        上の場合だとユーザーに適した画像を呼び出すことができる。 -->
        <h2><%=  current_user.nickname %>
        <!-- 現在ログインしているユーザーのニックネームを呼び出す。--><span>さんのマイページ</span></h2> -->
      </a>
    </div>
  </header>
  <div class="pt1em pb1em" id="contents">  <div id="main_cnt_wrapper">
    <div id="yjContentsBody">
      <div class="yjContainer">
        <ul class="user_page-contents">
            <%  current_user.reviews.each do |review| %>
            <!-- 現在ログインしているユーザーのレビューをcurrent_user.reviews(配列)で呼び出しeachメソッドをかけて要素を一つ一つ取り出す。 -->
              <li style="background-image: url(<%= review.product.image_url %>)">
              <!-- アソシエーションを利用してレビューに対応した映画の画像URLを呼び出す。 -->
                <div class="meta">
                  <header>
                    <div class="title"><%= review.product.title %>
                      <!-- アソシエーションを利用してレビューに対応した映画のタイトルを呼び出す。 -->
                    </div>
                    <span class="star"><span class="rating-star"><i class="star-actived rate-<%= review.rate %>0"></i></span></span>
                  </header>
                  <div class="user_review"><%= review.review %>
                    <!-- reviewモデルのreviewカラムからレビューの内容を取り出す。
                    ↑ review(モデル名).review(カラム名)である -->
                  </div>
                </div>
              </li>
            <% end %>
        </ul>

      </div>
    </div>
  </div>